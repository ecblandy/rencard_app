import { LocalDatePipeTsPipe } from './local-date.pipe.ts-pipe';

describe('LocalDatePipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalDatePipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
